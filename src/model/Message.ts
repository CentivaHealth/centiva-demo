export enum Message {
  EMPTY_SURVEY = "Survey hash is empty.",
  INVALID_SIGNATURE = "Invalid signature.",
  INSUFFICIENT_BALANCE = "Payout account has insufficient balance.",
  UNSUCCESSFUL = "Cannot send request to backend api.",
  SURVEY_CLAIMED = "Survey reward has already been claimed by account."
}
